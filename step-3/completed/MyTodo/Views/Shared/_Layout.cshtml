<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<title>@ViewBag.Title</title>
		<link href="@Styles.Url("~/assets/css/bootstrap.css")" rel="stylesheet" />
		<style>
		    body {
		        padding: 60px;
		    }
		</style>
	</head>
	<body>
		<div class="container">
			<div class="navbar navbar-fixed-top navbar-inverse">
				<div class="navbar-inner">
                    @if (Request.IsAuthenticated) {
						<p class="navbar-text pull-right">
							@User.Identity.Name	| 
                            @Html.ActionLink("выйти", "Logout", "User", null, new { @class = "navbar-link"})	
						</p>
                    }
                   </div>
			</div>
			<div class="page-header">
				<h2>@ViewBag.Title</h2>
			</div>
			@RenderBody()	
		</div>
		<script src="@Scripts.Url("~/assets/js/jquery-1.8.2.js")" type="text/javascript"></script>
		<script src="@Scripts.Url("~/assets/js/bootstrap.js")" type="text/javascript"></script>
	</body>
</html>
